Cloud deployment notes (AWS ECS / GCP Cloud Run):
- Set environment variables for provider credentials securely.
- Use IAM roles for AWS, service accounts for GCP.
- Use ECR/GCR for container images.
- Ensure poppler and tesseract binaries available in container.
